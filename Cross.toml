[build]
pre-build = [
    "apt-get update && apt-get install -y pkg-config libssl-dev && rm -rf /var/lib/apt/lists/*",
]

[target.x86_64-unknown-linux-gnu]
# Uses the default cross Docker image with OpenSSL
